{
  "name": "cafe-launch-control-centre",
  "private": true,
  "version": "1.0.0",
  "description": "Cafe Launch Control Centre - track opening a cafe in Malaysia from planning through opening day",
  "scripts": {
    "install:all": "npm install --include=dev && npm install --prefix server --include=dev && npm install --prefix client --include=dev",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
