# Copy this file to server/.env for local development, or set these as
# environment variables on your host (e.g. Render's dashboard).

# Postgres connection string. Get one for free from neon.tech or supabase.com.
DATABASE_URL=postgres://user:password@host:5432/dbname

# Port the API server listens on. Render sets this automatically in production.
PORT=4000
